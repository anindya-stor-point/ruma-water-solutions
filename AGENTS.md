# Agent Instructions

## Android App Icons
* **CRITICAL RULE:** NEVER modify, delete, or overwrite the `ic_launcher.png` files located in the `android/app/src/main/res/mipmap-*` directories. 
* The user has manually uploaded the correct "Ruma Water Solutions" logo in proper binary PNG format. 
* Do not run any scripts to regenerate these icons. Leave them exactly as they are.
